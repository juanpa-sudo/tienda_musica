{{> navbar}}
<div>
    <h1 class="py-5 text-center">
        Concierto
    </h1>
    <div class="container">
        <div class="row mb-5">
            {{#each respuesta }}
                {{>  concierto}}
            {{/each}}
        </div>
    </div>
</div>
{{> footer}}
