<div>
      {{> navbar}}
	<div class="container mt-5">
        {{#each respuesta }}
            
		<h1 class="text-center mb-5">Informacion de
			{{nombre}}
			{{apellidos}}</h1>
		<div class="row">
			<div class="col-md-5 me-3 d-flex align-items-center">
				<img
					src="/img/{{foto}}"
					alt=""
					class="w-100 rounded"
					width="400px"
				/>
			</div>
			<div
				class="col-md-6 d-flex justify-content-center align-items-ccenter flex-column"
			>
				<h2 class="text-center mb-2">Informacion Básica</h2>
				{{{biografia}}}
			</div>
		</div>
            <h2 class="text-center mt-5">Canciones de
			{{nombre}} {{apellidos}}</h2>
        {{/each}}
        {{#if canciones}}
		<div class="row">
            {{#each canciones }}
                {{> canciones}}
                
            {{/each}}
		</div>    
        {{else}}
            <h3 class="text-center">No Hay Canciones</h3>
        {{/if}}
		
	</div>
	{{> footer}}
</div>